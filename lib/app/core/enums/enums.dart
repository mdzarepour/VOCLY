enum WordScreenType {
  editWord,
  addWord
}

enum BookScreenType {
  editBook,
  addBook
}

enum ManageWordsScreenType {
  addWordToBook,
  manageWords
}

enum FilterType {
  color,
  type,
  icon,
  level
}

enum SortType {
  newest,
  oldest,
  aToZ,
  zToA
}
